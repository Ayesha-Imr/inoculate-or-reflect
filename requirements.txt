# Core reproducibility requirements for inoculate-or-reflect.
# The mechanistic notebook (notebooks/phase4_mechanistic_nnsight.ipynb) pins the
# versions below; the training/eval pipeline (kaggle/, eval/, training/) is
# compatible with the same stack.

# --- model + training ---
torch>=2.4
transformers>=4.51
peft>=0.14
bitsandbytes>=0.43        # 4-bit NF4 QLoRA (GPU only)
accelerate>=1.2
datasets>=2.19
safetensors>=0.4
huggingface_hub>=0.25

# --- mechanistic interpretability ---
nnsight>=0.7,<0.8         # residual-stream read/edit during generation

# --- grading / judging ---
openai>=1.40              # gpt-4.1-mini verdict judge (needs OPENAI_API_KEY)

# --- analysis + figures ---
numpy>=1.26
scikit-learn>=1.3         # AUROC direction sanity checks
matplotlib>=3.7           # paper figures
nbformat>=5.9             # notebooks/build_notebook.py

# NOTE
# - Phases 0-3 (data gen, reflection distillation, QLoRA training, generation)
#   were run on Kaggle T4/P100; Phase 4 (mechanistic) on Colab Pro A100 (High-RAM).
# - `google-colab` (Drive mount, userdata secrets) is provided by the Colab runtime
#   and is intentionally NOT listed here; the notebook degrades gracefully off-Colab.
# - Pin `transformers==5.13.1` to exactly reproduce the Phase-4 run (Qwen3 decoder
#   layers return the residual as a bare Tensor there); >=4.51 also works.
