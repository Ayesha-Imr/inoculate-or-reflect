# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Required for the gpt-4.1-mini verdict judge (eval/judge.py, phase-3 grading,
# and the mechanistic notebook's behavioral grading).
OPENAI_API_KEY=sk-...

# Hugging Face token — only needed to PUSH adapters/datasets, or to read private
# repos.
# HF_TOKEN=hf_...
